name: seu_app_name
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  image_picker: ^1.1.2
  google_fonts: ^6.2.1        # ← nova
  flutter_spinkit: ^5.2.1     # ← nova (loading bonitinho)

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/placeholder.jpg